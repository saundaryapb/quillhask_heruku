{
  "name": "Chart Socket Data",
  "version": "1.0.0",
  "description": "",
  "main": "chartsocket.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "binance": "^1.3.5",
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  }
}
